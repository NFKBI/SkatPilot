export default function Home() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Velkommen til SkatteSkræddersyeren</h1>
      <p>API'en er aktiv. Du kan nu modtage Stripe-webhooks på /api/webhook</p>
    </main>
  );
}
